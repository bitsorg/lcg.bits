--- a/./pythran/config.py
+++ b/./pythran/config.py
@@ -313,2 +313,2 @@
-    import distutils.ccompiler
-    import distutils.sysconfig
+    import setuptools._distutils.ccompiler
+    import sysconfig
@@ -369,2 +369,2 @@
-    compiler_obj = distutils.ccompiler.new_compiler()
-    distutils.sysconfig.customize_compiler(compiler_obj)
+    compiler_obj = setuptools._distutils.ccompiler.new_compiler()
+    sysconfig.customize_compiler(compiler_obj)
@@ -387 +387 @@
-            cflags.append('-I' + distutils.sysconfig.get_python_inc())
+            cflags.append('-I' + sysconfig.get_python_inc())
@@ -401 +401 @@
-            libs = distutils.sysconfig.get_config_var('LIBS')
+            libs = sysconfig.get_config_var('LIBS')
@@ -404,2 +404,2 @@
-            libdir = distutils.sysconfig.get_config_var('LIBDIR')
-            pylib = distutils.sysconfig.get_config_var('LDLIBRARY')
+            libdir = sysconfig.get_config_var('LIBDIR')
+            pylib = sysconfig.get_config_var('LDLIBRARY')
@@ -410 +410 @@
-                               + distutils.sysconfig.get_config_var('VERSION'))
+                               + sysconfig.get_config_var('VERSION'))
--- a/./pythran/tables.py
+++ b/./pythran/tables.py
@@ -4630,7 +4630,9 @@
             defaults = list(spec.defaults or [])
             args += [ast.Name(arg, ast.Param(), None, None)
                      for arg in spec.kwonlyargs]
-            defaults += [spec.kwonlydefaults[kw] for kw in spec.kwonlyargs]
+            if spec.kwonlydefaults:
+                defaults += [spec.kwonlydefaults[kw] for kw in spec.kwonlyargs
+                             if kw in spec.kwonlydefaults]
 
             # Check if we already have a pythran description for that object
             if signature.args.args:
