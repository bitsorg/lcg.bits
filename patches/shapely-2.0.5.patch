--- a/setup.py
+++ b/setup.py
@@ -156,6 +156,12 @@
 else:
     ext_options = get_geos_paths()
 
+    # Add the compile flag to suppress the incompatible-pointer-types warning
+    if 'extra_compile_args' not in ext_options:
+        ext_options['extra_compile_args'] = ['-Wno-error=incompatible-pointer-types']
+    else:
+        ext_options['extra_compile_args'].append('-Wno-error=incompatible-pointer-types')
+
     ext_modules = [
         Extension(
             "shapely.lib",
